# 线上环境平台打包路径 — served under /admin/
VITE_PUBLIC_PATH = /admin/

# 线上环境路由历史模式 (HTML5 with /admin/ base)
VITE_ROUTER_HISTORY = "h5,/admin/"

# 是否在打包时使用cdn替换本地库 替换 true 不替换 false
VITE_CDN = false

# 压缩
VITE_COMPRESSION = "none"

# API base
VITE_API_BASE_URL = /api
